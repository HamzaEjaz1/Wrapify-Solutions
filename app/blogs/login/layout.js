export const metadata = {
  title: "Admin Login | Wrapify Solutions",
  robots: { index: false, follow: false, googleBot: { index: false, follow: false } }
};

export default function BlogsLoginLayout({ children }) {
  return children;
}
